# 向量检索增强依赖 (可选)
# 安装: pip install -r requirements-vector.txt
# 
# 说明:
# - 安装后可使用向量检索功能,提升相似案例检索精度
# - 不安装时自动降级为文本匹配,基础功能不受影响
# - 首次使用会下载模型文件 (~80MB)

# 向量检索
sentence-transformers>=3.0.0

# 深度学习框架
torch>=2.0.0

# 数值计算
numpy>=1.24.0

# 向量相似度计算
scipy>=1.11.0
